# Rediska

### Compiling & running the project:
```bash
cc -Wall -Wextra -Og -g src/main.c -o build/main -Wimplicit-function-declaration && ./build/main
cc -Wall -Wextra -Og -g src/client.c -o build/client -Wimplicit-function-declaration && ./build/client
```
